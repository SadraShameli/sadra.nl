# `<Firm Name>` — Source Ledger

One row per primary-source document used anywhere in this firm's tree. Update
this file every time a source is added, re-fetched, or re-verified — this is
what makes a future re-verification pass possible without re-deriving which
document said what.

| Source URL    | Article's Own "Updated" Date       | Fetched/Pasted On | Used In             | Notes                                                             |
| ------------- | ---------------------------------- | ----------------- | ------------------- | ----------------------------------------------------------------- |
| `<exact URL>` | `<date the article itself states>` | `<YYYY-MM-DD>`    | `<plan-file(s).md>` | [e.g. "pasted as HTML by the user" / "fetched live via WebFetch"] |

Never add a row for a source that was summarized to you second-hand (a
research report, a third-party site, memory of an earlier session). Only a
document you or an agent directly read in full — pasted, fetched, or
read from disk — belongs here.
